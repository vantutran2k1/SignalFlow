apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "signalflow.fullname" . }}
  labels:
    {{- include "signalflow.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.env }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
